# OpenAI API key - server-side only. Never commit the real value.
# Used exclusively inside the /api routes (boss-decompose, boss-summary).
OPENAI_API_KEY=

# Model used for the two real Boss API calls.
# Defaults to gpt-5.4-mini for live-demo speed and cost control.
OPENAI_MODEL=gpt-5.4-mini
